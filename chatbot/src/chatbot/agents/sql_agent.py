"""SQL Agent (description only).

- Uses Semantic Kernel function calling to produce SQL given a semantic model.
- Enforces schema constraints from `sql_schema` container.
- Returns structured result + citations.
"""

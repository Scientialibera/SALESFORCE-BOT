"""Settings (description only).

- Read environment variables via Pydantic BaseSettings:
  AOAI_ENDPOINT, AOAI_DEPLOYMENT, AZURE_CLIENT_ID
  COSMOS_SQL_ENDPOINT, COSMOS_SQL_DB
  COSMOS_GRAPH_ENDPOINT, GRAPH_DB, GRAPH_NAME
- Provide computed config (timeouts, retry policies, RBAC toggles).
"""

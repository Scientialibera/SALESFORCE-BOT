{
  "name": "graph_agent",
  "description": "Graph agent for querying relationships, connections, and account information from the knowledge graph",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "User's natural language query about relationships, connections, or account information"
      },
      
      "accounts_mentioned": {
        "type": ["array", "null"],
        "items": {"type": "string"},
        "description": "List of account names/aliases mentioned in the query, or null if none."
      }
    },
  "required": ["query"]
  },
  "agents": ["graph_agent"],
    "metadata": {
    "agent_type": "graph",
    "category": "relationship_query",
    "underlying_functions": ["graph_agent_function", "account_resolver_function"],
    "data_sources": ["gremlin_graph"],
    "partition_key": "graph_agent"
  },
  "id": "graph_agent"
}

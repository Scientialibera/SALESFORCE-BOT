{
  "id": "graph_agent_function",
  "agent_type": "graph",
  "function_name": "graph_agent_function",
  "name": "graph_agent_function",
  "description": "Query the knowledge graph to find relationships between accounts, contacts, and business entities",
  "parameters": {
    "type": "object",
    "properties": {
      "account_name": {
        "type": "string",
        "description": "Name of the account to search for"
      },
      "relationship_type": {
        "type": "string",
        "description": "Type of relationship to explore (e.g., 'partner', 'competitor', 'supplier')",
        "enum": ["partner", "competitor", "supplier", "customer", "vendor"]
      },
      "max_depth": {
        "type": "integer", 
        "description": "Maximum traversal depth in the graph",
        "default": 2,
        "maximum": 5
      }
    },
    "required": ["account_name"]
  },
  "return_type": {
    "type": "object",
    "description": "Graph traversal results with nodes and relationships"
  },
  "examples": [
    {
      "input": {"account_name": "Salesforce", "relationship_type": "partner"},
      "description": "Find partner relationships for Salesforce"
    }
  ],
  "safety_guidelines": [
    "Limit traversal depth to prevent infinite loops",
    "Respect data access permissions",
    "Return only business-relevant relationships"
  ]
}

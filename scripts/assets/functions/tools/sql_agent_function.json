{
  "id": "sql_query_function",
  "agent_type": "sql",
  "function_name": "sql_query_function",
  "name": "sql_query_function",
  "description": "Execute SQL queries against the Fabric Lakehouse to retrieve sales and account data",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The SQL query to execute"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of rows to return",
        "default": 100
      }
    },
    "required": ["query"]
  },
  "return_type": {
    "type": "object",
    "description": "Query results with data rows and metadata"
  },
  "examples": [
    {
      "input": {"query": "SELECT TOP 10 * FROM accounts WHERE type = 'Customer'"},
      "description": "Get top 10 customer accounts"
    }
  ],
  "safety_guidelines": [
    "Only allow SELECT queries",
    "Enforce row limits to prevent large result sets",
    "No DROP, DELETE, UPDATE, or INSERT operations"
  ]
}
